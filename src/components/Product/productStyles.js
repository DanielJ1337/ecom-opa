import styled from "styled-components";

const ProductContainer = styled.div``;

const styles = {
  container: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    height: "80vh",
  },
};

export { ProductContainer, styles };
